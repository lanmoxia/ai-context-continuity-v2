# 新窗口启动演练

这份清单只用于开发前人工演练，不进入业务 Context Pack，也不要求任何窗口读取其他角色目录。

## 当前结果

- Codex 5.6 架构窗口：旧规则通过（2026-07-17）；架构所有者规则待复测。
- Codex 5.5 协调窗口：通过（2026-07-17）。
- Antigravity 开发窗口：通过（2026-07-17；首次路由错误修复后复测通过）。
- Antigravity 初审窗口：通过（2026-07-17）。

## 演练规则

- 每项都使用全新的窗口或 Conversation。
- 只输入对应启动文字，不粘贴旧聊天。
- 窗口必须简短说明自己的角色、当前状态和唯一下一步。
- 窗口不得读取其他角色私有目录，不得开始开发或审核。
- 如果回复不符合预期，关闭该窗口并回到 Codex 5.6 架构窗口修正规则。

## 1. Codex 5.6 架构窗口

输入：`启动56`

预期：识别为架构所有者模式；自行说明当前 Work Order 的技术结论，不要求用户审阅范围、风险或哈希；如已有 approved Work Order，直接给出一段可复制到 5.5 的短指令。

## 2. Codex 5.5 协调窗口

输入：`启动55`

预期：识别为协调、中审和指令分发窗口；只调度 5.6 已批准且用户当前短指令明确指定的 Work Order，不要求用户做技术审批。

## 3. Antigravity 开发窗口

输入：`启动开发`

预期：识别为开发窗口；说明 WORK-0001 已批准但当前没有 5.5 可执行指令；提醒模型必须由用户按未来 5.5 指令手动选择；不修改文件。

## 4. Antigravity 初审窗口

输入：`启动审核`

预期：识别为独立初审窗口；提醒用户手动选择 Gemini 3.5 Flash；说明当前没有 fresh Review Pack；不开始审核。

## 通过标准

四项都符合预期后，本次演练通过。关闭三个测试窗口；需要保留的正式窗口按各自 HANDOFF 继续使用。
