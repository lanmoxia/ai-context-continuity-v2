# BDEV-0001-r1

## 调度元数据

- 类型：Bootstrap development dispatch
- 调度 ID：BDEV-0001
- 修订：r1
- 生成时间：2026-07-17T16:57:58+08:00
- 准备者：Codex 5.6 架构迁移
- 调度责任角色：Codex 5.5
- 目标角色：Antigravity 开发窗口
- 目标模型：Claude Sonnet 4.6，由用户手动确认
- `supersedes`：未落盘且从未执行的 legacy inline WORK-0001 长指令
- 执行模式：BOOTSTRAP-L0；本文件不是正式 Context Pack

## 精确绑定

- Task：TASK-0001
- Stage：STAGE-01
- 计划项：DEV-001
- Work Order：WORK-0001
- Task 分支：`continuity/TASK-0001-project-foundation`
- 业务执行基线：`165ad65244fa3e5c31307433c6d7337b1e6fb0eb`
- 权威 Work Order：`docs/work-orders/DEV-001/work-order.json`
- Work Order SHA-256：`395e65fec671f5ae4dd81cc78c201a08c232d1a10bfa66aff32f78c938cf6290`

业务执行基线是本轮业务 diff 的起点。当前 HEAD 可以包含保存本调度文件和 5.5 HANDOFF 的后续纯会话控制提交，但不得包含其他业务变更。

## 唯一目标

执行已批准的 WORK-0001，建立可安装、可运行、可测试的 Python 工程骨架；不要实现后续业务能力。

## 读取顺序

完成“启动开发”的角色启动后，只按以下顺序读取：

1. `docs/work-orders/DEV-001/work-order.json` 全文。
2. `docs/spec/ARCHITECTURE.md` 的 `3. 工具仓库结构`。
3. `docs/spec/CLI_SPEC.md` 的 `1. 项目`。

不要读取 `work-order.md`、其他规格章节、其他调度修订或审核结果。允许路径、禁止路径、交付物、检查和验收标准全部以 Work Order JSON 为唯一事实来源，本文件不重复定义。

## 开始核对

开始写业务代码前必须确认：

1. 短启动块给出的本文件 SHA-256 与实际文件一致。
2. 当前分支名称与上述 Task 分支一致，且业务执行基线是当前 HEAD 的祖先。
3. 从业务执行基线到开始执行前，只允许以下会话控制文件发生变化：
   - `.continuity/session-control/dispatches/development/BDEV-0001-r1.md`
   - `.continuity/session-control/chatgpt/codex-5.5/HANDOFF.md`
4. 工作区没有未提交修改。
5. Work Order 状态为 `approved`，且文件 SHA-256 匹配。
6. 用户已把 Antigravity 开发窗口手动切换为 Claude Sonnet 4.6，且没有其他业务代码写入者。

任一项不满足立即停止，不得自行修正基线或扩大范围。旧的 inline 长指令已经作废，不得合并执行。

## 执行与完成

- 使用项目 `.venv` 和 Python 3.11+。
- 按 Work Order JSON 的 `required_checks` 中 `argv` 与 `env` 执行；在 Windows PowerShell 中正确设置环境变量，不改变命令语义。
- 业务修改严格限制在 Work Order `allowed_paths`。唯一会话控制写入例外是更新 `.continuity/session-control/antigravity/development/HANDOFF.md`。
- 不修改、覆盖或删除本调度文件，也不创建下一条调度。
- 如果需要修改允许范围外文件、发现规格冲突、出现未知写入者或检查无法运行，停止并把阻塞写入自己的 HANDOFF。
- 完成时在自己的 HANDOFF 记录调度 ID、路径、短启动块提供的 SHA-256、修改文件和全部检查结果，然后告诉用户回到 Codex 5.5 窗口输入 `kf`。
