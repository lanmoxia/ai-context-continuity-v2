# BDEV-0002-r1

## 调度元数据

- 类型：Bootstrap development dispatch
- 调度 ID：BDEV-0002
- 修订：r1
- 生成时间：2026-07-18T13:12:43+08:00
- 准备者：Codex 5.5
- 调度责任角色：Codex 5.5
- 目标角色：Antigravity 开发窗口
- 目标模型：Claude Opus 4.6，由用户手动确认
- `supersedes`：无
- 执行模式：BOOTSTRAP-L0；本文件不是正式 Context Pack

## 精确绑定

- Task：TASK-0001
- Stage：STAGE-02
- 计划项：DEV-002
- Work Order：WORK-0002
- Task 分支：`continuity/TASK-0001-project-foundation`
- 业务执行基线：`750d3ec7a144e958c62f6f45fa8a99cbf1232a5b`
- 权威 Work Order：`docs/work-orders/DEV-002/work-order.json`
- Work Order SHA-256：`9249ab044d29f761060c0aa1224630018a3b9aec143ab333aae0fced53c14714`

业务执行基线是本轮业务 diff 的起点。当前 HEAD 可以包含保存本调度文件和 5.5 HANDOFF 的后续纯会话控制提交，但不得包含其他业务变更。

## 唯一目标

执行已批准的 WORK-0002，为 Continuity 首批持久化实体建立可打包、可离线、默认拒绝未知输入的 JSON Schema 合同和固定白名单加载器。

## 读取顺序

完成“启动开发”的角色启动后，只按以下顺序读取：

1. `docs/work-orders/DEV-002/work-order.json` 全文，并以它作为唯一 Work Order 权威来源。
2. `docs/spec/DATA_MODEL.md` 的以下章节：
   - `1. 关系`
   - `2. 当前状态`
   - `2.1 Project 与 Config`
   - `2.2 Source Fingerprint`
   - `3. Task`
   - `4. Stage`
   - `5. Work Order`
   - `6. Checkpoint`
   - `7. Handoff`
   - `8. Evidence`
   - `9. Review Pack`
   - `10. Context Pack`
   - `14. 不可破坏的约束`
3. `docs/spec/ARCHITECTURE.md` 的以下章节：
   - `3. 工具仓库结构`
   - `4. 业务项目状态目录`
   - `5. 单一事实来源`
   - `9. 配置和版本`
4. `docs/spec/REQUIREMENTS.md` 的以下章节：
   - `2.2 长任务分阶段`
   - `2.3 窗口接力`
   - `2.5 分阶段审核`
   - `2.6 审核可信`
   - `2.9 状态一致`
   - `2.10 任务并发边界`
   - `2.11 配置与生命周期`
   - `3. 安全要求`
   - `4. 非功能要求`
5. `docs/spec/CONFIGURATION.md` 的以下章节：
   - `1. 配置文件`
   - `2. 检查命令`
   - `4. 版本迁移`
   - `6. Windows 兼容`
6. `docs/spec/CORE_WORKFLOW.md` 的以下章节：
   - `2. 创建大任务`
   - `4. 生成并读取 Context Pack`
   - `6. 正常换窗口`
   - `9. Stage 完成和审核`
   - `10. Task 总体验收与合并`
7. `docs/spec/REVIEW_POLICY.md` 的以下章节：
   - `1. 两种审核范围`
   - `2. Stage Review Gate`
   - `3. 风险等级`
   - `4. 必要检查`
   - `5. Finding 分级`
   - `7. Task Final Review`

读取规格文件时，只读取列出的章节正文到下一个同级或更高层级标题为止。若章节缺失、标题含义不确定或需要扩大读取范围才能实现，立即停止并把歧义写入自己的 HANDOFF。

不要读取 `work-order.md`、其他规格章节、其他调度修订或审核结果。允许路径、禁止路径、交付物、检查和验收标准全部以 Work Order JSON 为唯一事实来源，本文件不重复定义。

## 开始核对

开始写业务代码前必须确认：

1. 短启动块给出的本文件 SHA-256 与实际文件一致。
2. 当前分支名称与上述 Task 分支一致，且业务执行基线是当前 HEAD 的祖先。
3. 从业务执行基线到开始执行前，只允许以下会话控制文件发生变化：
   - `.continuity/session-control/dispatches/development/BDEV-0002-r1.md`
   - `.continuity/session-control/chatgpt/codex-5.5/HANDOFF.md`
4. 工作区没有未提交修改。
5. Work Order 状态为 `approved`，且文件 SHA-256 匹配。
6. 用户已把 Antigravity 开发窗口手动切换为 Claude Opus 4.6，且没有其他业务代码写入者。
7. Python 版本满足 Work Order 的要求。

任一项不满足立即停止，不得自行修正基线或扩大范围。

## 调度前已存在且允许保留的脏文件

无。调度生成前工作区为 clean。

## 本轮允许的会话控制写入

开发窗口唯一允许写入的会话控制文件：

- `.continuity/session-control/antigravity/development/HANDOFF.md`

该文件是开发窗口接力摘要例外，不属于 Work Order 业务交付。不得修改、覆盖或删除本调度文件，不得修改其他角色 HANDOFF，不得创建下一条调度、审核材料或产品核心 `.continuity` 状态。

## 执行与完成

- 使用项目专用且已激活的 Python 3.11+ 虚拟环境。
- 按 Work Order JSON 的 `required_checks` 中 `argv` 与 `env` 执行；在 Windows PowerShell 中正确设置环境变量，不改变命令语义。
- 业务修改严格限制在 Work Order `allowed_paths`。
- 如果需要修改允许范围外文件、发现规格冲突、出现未知写入者、检查无法运行或 required checks 出现未授权 skipped，停止并把阻塞写入自己的 HANDOFF。
- 完成时在自己的 HANDOFF 记录调度 ID、路径、短启动块提供的 SHA-256、修改文件、全部检查结果、测试数量和 skipped 数量，然后告诉用户回到 Codex 5.5 窗口输入 `kf`。
