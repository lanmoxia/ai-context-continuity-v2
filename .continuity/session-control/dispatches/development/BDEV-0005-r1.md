# BDEV-0005-r1

## 调度元数据

- 类型：Bootstrap development rework dispatch
- 调度 ID：BDEV-0005
- 修订：r1
- 生成时间：2026-07-20T11:15:30+08:00
- 准备者：Codex 5.5
- 调度责任角色：Codex 5.5
- 目标角色：Antigravity 开发窗口
- 目标模型：Gemini 3.5 Flash (High)，由用户手动确认
- `continues_from`：`.continuity/session-control/dispatches/development/BDEV-0004-r1.md`
- `continues_from` SHA-256：`27836a6da29d8c41317b04e0f68044631a2c6f906c3b07f88e12b41328e14517`
- 执行模式：BOOTSTRAP-L0；本文件不是正式 Context Pack

## 精确绑定

- Task：TASK-0001
- Stage：STAGE-02
- 计划项：DEV-002
- Work Order：WORK-0002
- Task 分支：`continuity/TASK-0001-project-foundation`
- 业务执行基线：`750d3ec7a144e958c62f6f45fa8a99cbf1232a5b`
- 当前调度保存提交：5.5 创建并推送本文件后的 HEAD
- 权威 Work Order：`docs/work-orders/DEV-002/work-order.json`
- Work Order SHA-256：`9249ab044d29f761060c0aa1224630018a3b9aec143ab333aae0fced53c14714`
- 前一返工调度：`.continuity/session-control/dispatches/development/BDEV-0004-r1.md`
- 前一返工调度 SHA-256：`27836a6da29d8c41317b04e0f68044631a2c6f906c3b07f88e12b41328e14517`

业务执行基线仍是本轮业务 diff 的起点。当前工作区包含未提交 WORK-0002 实现修改和开发窗口接力摘要修改。Gemini High 必须保留全部既有改动，在当前脏工作区上补齐证据，不得 reset、stash、丢弃、清理或重新基于空白实现。

## 唯一目标

补齐 WORK-0002 返工证据，使 `BDEV-0004-r1` 修复具有持久单元测试覆盖，并把开发窗口自己的 HANDOFF 更新为当前调度完成记录。若补测试暴露代码行为仍不满足 Work Order，必须在 allowed paths 内修复后重新运行全部 required checks。

## 读取顺序

完成“启动开发”的角色启动后，只按以下顺序读取：

1. 本调度文件全文，并核对短启动块中的 SHA-256。
2. `docs/work-orders/DEV-002/work-order.json` 全文，并以它作为唯一 Work Order 权威来源。
3. `tests/unit/test_schemas.py`、`tests/fixtures/schemas/**`、`src/continuity/schemas/registry.py` 和相关 schema 文件。
4. 仅当需要核对字段语义时，读取 `docs/spec/DATA_MODEL.md` 中 Source Fingerprint、Review Pack / Review Decision 和相关实体字段附近内容。

不要读取 `work-order.md`、审核结果、历史审核材料或其他角色 HANDOFF。允许路径、禁止路径、交付物、检查和验收标准全部以 Work Order JSON 为唯一事实来源，本文件不重复定义。

## 调度前已存在且允许保留的精确脏文件

以下文件来自已执行但尚未通过 5.5 预检的 WORK-0002 实现现场，本轮返工必须保留并可继续修改：

- `.continuity/session-control/antigravity/development/HANDOFF.md`（开发窗口接力摘要例外）
- `pyproject.toml`
- `src/continuity/schemas/__init__.py`
- `src/continuity/schemas/checkpoint.schema.json`
- `src/continuity/schemas/common.schema.json`
- `src/continuity/schemas/config.schema.json`
- `src/continuity/schemas/context-pack.schema.json`
- `src/continuity/schemas/current.schema.json`
- `src/continuity/schemas/evidence.schema.json`
- `src/continuity/schemas/handoff.schema.json`
- `src/continuity/schemas/project.schema.json`
- `src/continuity/schemas/registry.py`
- `src/continuity/schemas/review-decision.schema.json`
- `src/continuity/schemas/review-pack.schema.json`
- `src/continuity/schemas/stage.schema.json`
- `src/continuity/schemas/task.schema.json`
- `src/continuity/schemas/work-order.schema.json`
- `tests/fixtures/schemas/invalid/review_decision_bad_verdict.json`
- `tests/fixtures/schemas/invalid/review_pack_bad_sha256.json`
- `tests/fixtures/schemas/invalid/task_bad_enum.json`
- `tests/fixtures/schemas/invalid/task_bad_id.json`
- `tests/fixtures/schemas/invalid/task_missing_fields.json`
- `tests/fixtures/schemas/invalid/task_no_timezone.json`
- `tests/fixtures/schemas/invalid/task_unknown_field.json`
- `tests/fixtures/schemas/invalid/task_wrong_type.json`
- `tests/fixtures/schemas/valid/checkpoint.json`
- `tests/fixtures/schemas/valid/config.json`
- `tests/fixtures/schemas/valid/context-pack.json`
- `tests/fixtures/schemas/valid/current.json`
- `tests/fixtures/schemas/valid/evidence.json`
- `tests/fixtures/schemas/valid/handoff.json`
- `tests/fixtures/schemas/valid/project.json`
- `tests/fixtures/schemas/valid/review-decision.json`
- `tests/fixtures/schemas/valid/review-pack.json`
- `tests/fixtures/schemas/valid/stage.json`
- `tests/fixtures/schemas/valid/task.json`
- `tests/fixtures/schemas/valid/work-order.json`
- `tests/unit/test_schemas.py`

Ignored build/cache outputs from required checks may exist and must remain ignored.

## 5.5 预检结果

5.5 收到 `kf` 后核对 `BDEV-0004-r1`，并重新运行 WORK-0002 的全部 14 项 required checks。结果：

- PYTHON_VERSION、EDITABLE_INSTALL、DEPENDENCY_CHECK、COMPILE、UNIT、SCHEMA_SELF_CHECK、SCHEMA_PACKAGE_RESOURCES、WHEEL_PREP、WHEEL_BUILD、WHEEL_SCHEMA_RESOURCES、MODULE_HELP、MODULE_VERSION、CONSOLE_HELP、CONSOLE_VERSION 均退出码 0。
- UNIT：36 tests，0 failures，0 skipped。
- 5.5 额外行为探针显示 `BDEV-0004-r1` 的主要代码路径有修复迹象：非法 RFC 3339 日期/时间/时区被拒绝，current 与 review-pack 非法引用/hash 被拒绝，Source Fingerprint `size_bytes` 约束生效，Review Decision `round` 必填，外部/文件/未登记 `$ref` 被拒绝。
- 但 required checks 通过不等于验收通过；以下 Findings 必须修复后才可冻结 BPACK。

## 已确认 Findings

### F55-WORK-0002-06 — high — 开发完成记录未更新到当前调度

证据：

- `.continuity/session-control/antigravity/development/HANDOFF.md` 仍记录当前调度为 `.continuity/session-control/dispatches/development/BDEV-0002-r1.md`，SHA-256 为 `8befafe11389dbf97ca1dc9cd0ebf75d120df6da4c5dd8db6880208325cf94d4`。
- 该 HANDOFF 仍记录模型为 Claude Opus 4.6，未记录 `BDEV-0004-r1` 或 Gemini 3.5 Flash (High)。
- `BDEV-0004-r1` 要求完成时更新开发窗口自己的 HANDOFF，记录调度 ID、路径、短启动块 SHA-256、修复内容、修改文件和 fresh checks。

修复要求：

- 本轮完成时更新 `.continuity/session-control/antigravity/development/HANDOFF.md`，记录 `BDEV-0005-r1`、本调度 SHA-256、目标模型 Gemini 3.5 Flash (High)、已补测试/修复内容、修改文件、全部 fresh required checks、UNIT 测试数量和 0 skipped。
- 不得修改其他角色 HANDOFF，不得修改调度文件、审核结果或产品核心 `.continuity` 状态。

### F55-WORK-0002-07 — high — `BDEV-0004-r1` 要求的回归测试未固定到 `test_schemas.py`

证据：

- `tests/unit/test_schemas.py` 仍为 36 tests，缺少针对 `BDEV-0004-r1` 的新增回归测试。
- 文件中没有覆盖语义非法日期/时间/时区的测试，例如 `2026-99-99T99:99:99+99:99`、`2026-02-30T10:00:00+08:00`、`2026-07-20T24:00:00+08:00` 或非法 offset。
- 文件中没有覆盖非空 Source Fingerprint 的 `size_bytes` 缺失、负数和错误类型拒绝。
- 文件中没有覆盖 current 多个引用 ID、review-pack 的 `stage_id`、`stage_sha256`、`source_fingerprint_sha256` 等非法引用/hash。
- 文件中没有覆盖 Review Decision 缺少通用审核轮次编号 `round` 的拒绝。
- 文件中没有实际证明外部 URI `$ref`、`file://` 引用、相对本地文件引用和未登记 URN 引用在任何网络或项目任意文件读取前被拒绝；现有测试只扫描包内 schema 文本是否包含 http(s) `$ref`。

违反：

- WORK-0002 验收要求非法 ID/时间、引用字段、Source Fingerprint 结构、结构化错误和 `$ref` 安全边界都有测试证明。
- `BDEV-0004-r1` 明确要求增加这些回归测试。

修复要求：

- 在 `tests/unit/test_schemas.py` 增加 durable 回归测试；可以使用 inline instances 或新增 `tests/fixtures/schemas/invalid/**` fixture。
- 测试必须直接断言上述非法输入被 `validate()` 或 registry 边界检查拒绝，不得只靠注释、字符串搜索或 5.5 的临时探针。
- 外部 URI、`file://`、相对本地文件和未登记 URN `$ref` 的测试必须证明拒绝发生在任何网络或项目文件读取前。可以通过 monkeypatch `importlib.resources.files`、私有 guard 函数或等价可维护方式验证，但不得访问网络或任意项目路径。
- 补完测试后重新运行全部 required checks，UNIT 必须记录新的测试数量且 0 skipped。

## 本轮允许的会话控制写入

开发窗口唯一允许写入的会话控制文件：

- `.continuity/session-control/antigravity/development/HANDOFF.md`

该文件是开发窗口接力摘要例外，不属于 Work Order 业务交付。不得修改、覆盖或删除本调度文件，不得修改其他角色 HANDOFF，不得创建下一条调度、审核材料或产品核心 `.continuity` 状态。

## 执行与完成

- 业务修改仍严格限制在 Work Order `allowed_paths`。
- 重点预计是测试和开发 HANDOFF；若新增测试暴露代码仍有缺陷，可在 `src/continuity/schemas/**` 内修复。
- 修复后重新运行 Work Order JSON 的全部 `required_checks`，记录每项退出码。
- `UNIT` 必须明确记录测试数量和 `0 skipped`。
- 完成时更新开发窗口自己的 HANDOFF，记录本调度 ID、路径、短启动块 SHA-256、修复内容、修改文件和 fresh checks。
- 完成后告诉用户回到 Codex 5.5 窗口输入 `kf`。
