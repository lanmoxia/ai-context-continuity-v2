# BDEV-0012-r1

## 调度元数据

- 类型：Bootstrap development rework dispatch
- 调度 ID：BDEV-0012
- 修订：r1
- 生成时间：2026-07-23T14:30:38+08:00
- 准备者：Codex 5.5
- 调度责任角色：Codex 5.5
- 目标角色：Antigravity 开发窗口
- 目标模型策略：`user_selected_available_model`
- `supersedes`：`.continuity/session-control/dispatches/development/BDEV-0011-r1.md`
- `supersedes` SHA-256：`b1b423dcd290cb97ce65176280d44ac823d1c11d84c2b9b42642adcc77526441`
- 执行模式：BOOTSTRAP-L0；本文件不是正式 Context Pack

## 精确绑定

- Task：TASK-0001
- Stage：STAGE-02
- 计划项：DEV-002
- Work Order：WORK-0003
- Task 分支：`continuity/TASK-0001-project-foundation`
- Stage 计划：`.continuity/session-control/stage-plans/BSTAGEPLAN-0001-r1.json`
- Stage 计划 SHA-256：`4213ace8c0758c1e958223e7e3c7aaf0aecfcffb62111eb94c049c92eb2dd820`
- 权威 Work Order：`docs/work-orders/WORK-0003/work-order.json`
- Work Order SHA-256：`8409492acd277568420af81e741a062210ef47b87e1f96edd5f534a361766dc9`
- Stage 计划上一项：WORK-0002，已完成
- WORK-0002 completion checkpoint：`67c9d6471b7f8cba17a2ca41fe02aea300c07a34`
- WORK-0002 completion commit：`1173d9d8b06934797b6977b0eebfe5a4db8032c3`
- WORK-0003 当前未验收实现检查点：`728717f2a641f589ac2c8575f2522c3ae778e33e`
- 本轮业务执行基线：`67c9d6471b7f8cba17a2ca41fe02aea300c07a34`
- Stage 累计实现 diff base：`83f3c21dcf2f58d99c9c898e5cd85c07f4e4625c`
- 当前调度保存提交：由 Codex 5.5 创建并推送本文件后的 HEAD

`BDEV-0012-r1` 是 WORK-0003 当前唯一有效 development 返工入口。保留 `BDEV-0010-r1` 和 `BDEV-0011-r1` 已产生并提交的全部业务改动，在当前实现检查点基础上增量修复；不要回滚、重做或改写 WORK-0002 历史结果。不要冻结 Stage BPACK，不要执行任何 Gate，不要创建新的调度或审核材料。

## 唯一目标

修复 Stage 累计实现 diff 的空白错误：`src/continuity/domain/invariants.py` 在从 Stage diff base 到当前检查点的 diff 中触发 `git diff --check` 的 `new blank line at EOF`。完成后按 Work Order JSON 原始 `required_checks` 全部重跑，并额外确认 Stage 累计 diff check 通过。

## 读取顺序

完成“启动开发”的角色启动后，只按以下顺序读取：

1. 本调度文件全文，并核对短启动块中的 SHA-256。
2. `.continuity/session-control/stage-plans/BSTAGEPLAN-0001-r1.json` 全文，并核对 SHA-256；只用于确认 WORK-0003 是当前 ready 且 terminal 的 Stage 计划项。
3. `docs/work-orders/WORK-0003/work-order.json` 全文，并核对 SHA-256；它仍是本轮范围、输入、输出、检查和验收标准的唯一 Work Order 权威来源。
4. `src/continuity/domain/invariants.py`，必要时读取 `tests/unit/test_domain.py`。

不要读取其他角色 HANDOFF、旧 BPACK、旧 Gate 结果、initial-review/pre-final-review 历史材料或聊天摘要。不要读取或修改 `.continuity/session-control/chatgpt/codex-5.6/HANDOFF.md`。

## 调度前已确认事实

- Stage 计划状态为 `approved`，`next_ready_work_order_id` 为 `WORK-0003`。
- Stage 计划中 WORK-0003 的 SHA-256 与当前文件复算值匹配。
- WORK-0003 状态为 `approved`，审批人为 `architecture_owner`。
- BDEV-0011 返工后的 WORK-0003 required checks 已由 5.5 在项目 `.venv` 中 fresh 重跑通过；UNIT 为 126 tests、0 skipped。
- WORK-0002 的 14 项 required checks 也已由 5.5 在项目 `.venv` 中 fresh 重跑通过；UNIT 为 126 tests、0 skipped；wheel 资源检查通过。
- 返工 Finding F-55-KF-001 至 F-55-KF-004 的探针已通过。
- 但 Stage 冻结前的累计实现 diff check 失败，因此不得冻结 BPACK。
- 当前工作区保留 `.continuity/session-control/chatgpt/codex-5.6/HANDOFF.md` 的既有会话控制修改；开发窗口必须保留但不得读取、修改、暂存或覆盖。

## 允许的写入范围

业务代码修改严格限制在 Work Order JSON 的 `allowed_paths` 内，并避开其中 `forbidden_paths`。本轮预计只涉及：

- `src/continuity/domain/invariants.py`

如修复后测试需要同步调整，仍只允许：

- `tests/unit/test_domain.py`

开发窗口唯一允许写入的会话控制文件：

- `.continuity/session-control/antigravity/development/HANDOFF.md`

该 HANDOFF 是开发窗口接力摘要例外，不属于 Work Order 业务交付。不得修改本调度文件、Stage 计划、Work Order、BPACK、Gate 结果、5.5 HANDOFF、5.6 HANDOFF 或产品核心 `.continuity` 状态。

## 已确认 Finding

### F-55-KF-005 - medium - Stage 累计实现 diff 存在 EOF 多空行

最小证据：

```text
git diff --check 83f3c21dcf2f58d99c9c898e5cd85c07f4e4625c 728717f2a641f589ac2c8575f2522c3ae778e33e -- pyproject.toml src/continuity/schemas tests/fixtures/schemas tests/unit/test_schemas.py src/continuity/domain tests/unit/test_domain.py
```

返回：

```text
src/continuity/domain/invariants.py:663: new blank line at EOF.
```

影响：

- Work Order 普通 `git diff --check` 在工作区干净时通过，但 Stage BPACK 要冻结的是从 Stage cumulative base 到实现检查点的原始 diff；该累计 diff 不能带空白错误证据进入 BPACK。
- 当前问题只影响 `src/continuity/domain/invariants.py` 文件末尾格式，不要求改变领域行为。

修复要求：

- 删除 `src/continuity/domain/invariants.py` 文件末尾多余空白行，确保文件以单个换行结束。
- 不改变业务逻辑，除非现有格式修复自然要求。
- 修复后运行 WORK-0003 全部 required checks、`git diff --check`，并额外运行上方 Stage cumulative diff check，三者都必须 exit 0。
- 更新开发窗口自己的 HANDOFF，记录本调度、修复、检查结果和未触碰 5.6 HANDOFF。

## 执行要求

- 在项目专用且已激活的 Python 3.11+ `.venv` 中执行；若无法确认 `.venv` 已激活，停止并在开发 HANDOFF 中报告。
- 不得修改 `pyproject.toml`、CLI、schema、storage、services、fixtures、docs 或任何 Work Order 禁止路径。
- domain 层不得复制 JSON Schema 字段模型，不读写文件，不访问网络、Git、时间、环境变量或当前工作目录。
- 领域错误必须以稳定结构返回，不打印 traceback，不用 assert 或进程退出表达调用方错误。
- 所有 `DomainViolation` 必须按 `(path, code)` 稳定排序。
- 完成后按 Work Order JSON 原始 `argv` 与 `env` 运行全部 `required_checks`，记录每项退出码；`UNIT` 必须记录测试数量和 `0 skipped`。
- 完成后运行 `git diff --check` 并记录退出码。
- 完成后运行 Stage cumulative diff check：

```text
git diff --check 83f3c21dcf2f58d99c9c898e5cd85c07f4e4625c HEAD -- pyproject.toml src/continuity/schemas tests/fixtures/schemas tests/unit/test_schemas.py src/continuity/domain tests/unit/test_domain.py
```

## 允许保留但不得触碰的现场

- `BDEV-0010-r1`、`BDEV-0011-r1` 后已经提交的 WORK-0003 业务改动
- `.continuity/session-control/chatgpt/codex-5.6/HANDOFF.md`
- ignored build/cache 输出

这些文件不得进入调度提交；5.6 HANDOFF 不得进入业务 diff，不得暂存。

## 完成回传

开发返工完成后，更新 `.continuity/session-control/antigravity/development/HANDOFF.md`，记录：

- 本调度 ID、路径和 SHA-256
- Stage 计划路径和 SHA-256
- Work Order 路径和 SHA-256
- 修复的 Finding ID：F-55-KF-005
- 修改文件
- required checks 逐项结果
- UNIT 测试数量与 `0 skipped`
- `.venv` 环境事实
- `git diff --check` 结果
- Stage cumulative diff check 结果
- 未触碰 5.6 HANDOFF 的说明

然后请用户回到 Codex 5.5 窗口输入 `kf`。
