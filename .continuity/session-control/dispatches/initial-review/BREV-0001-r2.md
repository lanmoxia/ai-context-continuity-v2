# BREV-0001-r2

## 调度元数据

- 类型：Bootstrap initial-review dispatch
- 调度 ID：BREV-0001
- 修订：r2
- 生成时间：2026-07-18T08:58:18+08:00
- 准备者：Codex 5.6 架构修复
- 调度责任角色：Codex 5.5
- 目标角色：Antigravity 初审窗口
- 目标模型：Gemini 3.5 Flash，由用户手动确认
- `supersedes`：`BREV-0001-r1.md`；r1 已输出但用户在粘贴到初审窗口前暂停，未生成审核结果
- 修订原因：r1 与初审隔离规则冲突，且把不可独立复算的审核材料内嵌在调度中
- 执行模式：BOOTSTRAP-L0；本文件不是正式 Review Pack

## 精确绑定

- Task：TASK-0001
- Stage：STAGE-01
- Work Order：WORK-0001
- Task 分支：`continuity/TASK-0001-project-foundation`
- 业务执行基线：`165ad65244fa3e5c31307433c6d7337b1e6fb0eb`
- Bootstrap Pack：`.continuity/session-control/bootstrap-packs/BPACK-0001.json`
- BPACK SHA-256：`6676d9cb34088b20732df13ddb06225bfb87d58b48089378e3b20c2c673021f4`
- 唯一结果文件：`.continuity/session-control/review-results/gate-01/BGATE01-0001-r1.md`

## 唯一目标

对当前 BPACK 执行 GATE-01 独立初审，并把结论写入唯一结果文件。

## 执行

1. 完成“启动审核”的固定角色启动。
2. 核对本调度文件 SHA-256。
3. 核对并完整读取上面指定的唯一 BPACK。
4. 按 BPACK 保存的算法复算 diff 与 Source Fingerprint。
5. 只按 BPACK 的精确路径和行范围读取审核输入，不读取开发窗口 HANDOFF、聊天摘要、历史调度或其他审核结论。
6. 按初审角色规则写入结果文件并更新自己的 HANDOFF。

调度不复制 BPACK 的文件清单、检查表、指纹、审核重点或结果字段；这些事实分别以 BPACK、Work Order 和初审角色规则为准。

## 停止条件

- 本调度或 BPACK 的实际 SHA-256 不匹配。
- BPACK 中任一 diff、文件条目或 Source Fingerprint 无法复算或不匹配。
- 当前分支不包含 BPACK 绑定的 implementation checkpoint，或业务文件相对该快照已经变化。
- BPACK 要求读取的文件缺失、完整文件哈希不匹配，或必须扩大输入范围才能下结论。
- 唯一结果文件已经存在。

除“结果文件已经存在”外，任一条件出现时按角色规则写入 `blocked` 结果并停止。若结果文件已经存在，不得覆盖，只更新自己的 HANDOFF 并向用户报告阻塞。任何情况下都不得自行修复业务代码、Pack 或调度。

## 完成回传

结果文件和初审 HANDOFF 均写入后，告诉用户回到 Codex 5.5 窗口输入 `sh`。
